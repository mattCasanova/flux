[package]
name = "flux-shell"
description = "Shell abstraction layer for Flux — zsh, bash, fish support"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
dirs = { workspace = true }
log = { workspace = true }
anyhow = { workspace = true }
