# Cargo build output
/debug
/target
**/*.rs.bk
*.pdb
**/mutants.out*/

# rust-analyzer (non-cargo build systems)
rust-project.json

# Cargo.lock IS committed — Flux is a binary (reproducible builds).

# JetBrains / RustRover
/.idea

# macOS
.DS_Store
._*

# Git merge-conflict backups
*.orig
*.BACKUP.*
*.BASE.*
*.LOCAL.*
*.REMOTE.*

# Claude Code local config (machine-specific)
.claude/settings.local.json
